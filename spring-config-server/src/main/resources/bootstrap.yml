server:
  port: 8888
  
spring:
  application:
    name: config-server
  cloud:
    bus:
      destination: test-bus
    config:
      server:
        git:
          uri: https://github.com/rathilavkesh/spring-config.git
#          username: ${GIT_URI_USERNAME}
#          password: ${GIT_URI_PASSWORD}
  rabbitmq:
    host: RABBITMQ_IP
#    port: 5672
#    username: lavkesh
#    password: lavkesh@123

encrypt:
  key: 36E2D4954B9F6EFB2BC397D1D3CBD
  
eureka:
  client:
    registry-fetch-interval-seconds: 20
    serviceUrl:
      defaultZone: http://test-eureka:test-password@SERVER_REGISTRY_IP:8761/eureka/
  instance:
    preferIpAddress: false
    
management:
  context-path: /manage
  port: 9080

endpoints:
  enabled: false
  sensitive: true
    
    

    
    
    
  
  
            